#include <stdio.h>
#include "main.h"
/**
*print_square - print line the number of times inputed
*@size: parameter
*
*Return: Void
*/
void print_square(int size)
{
	int i = 0;

	while (i < size)
	{
		int y = 0;

		while (y < size)
		{
			_putchar(35);
			y++;
		}
		_putchar(10);
		i++;
	}
	if (n <= 0)
		_putchar(10);
}
	_putchar(10);
}
