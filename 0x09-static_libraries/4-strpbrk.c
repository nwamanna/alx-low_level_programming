#include <stdio.h>
#include "main.h"
/**
*_strpbrk - calculates lenght of string
*@s: pointer
*accept: pointer
*
*Return: integer
*/
char *_strpbrk(char *s, char *accept)
{
	(void)s;
	(void)accept;
	return (s);
}
