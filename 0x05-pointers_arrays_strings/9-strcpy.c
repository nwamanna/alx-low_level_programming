#include <stdio.h>
/**
* *_strcpy - copy string
*@src: pointer
*@dest: pointer
*
*Return: the pointer to dest
*/
char *_strcpy(char *dest, char *src)
{
	int i;
	int k;

	for (i = 0; src[i] != '\0'; i++)
	{
	}
	for (k = 0; k <= length; k++)
	{
		dest[k] = src[k];
	}
	return (dest);
}
