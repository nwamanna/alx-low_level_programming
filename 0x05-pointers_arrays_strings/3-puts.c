#include <stdio.h>
/**
 * _puts - value of a to b and b to a
 * @str: pointer to an string
 *
 * Return: Void
 */
void _puts(char *str)
{
	int i;
	int sum = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
		char k = s[i];

		_putchar(k);
	}
	_putchar(10);
}
