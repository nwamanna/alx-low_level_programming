#include <stdio.h>
/**
*_abs- prints absolute value
*@int: the parameter
*
*Return: r if suceesful
*/
int _abs(int)
{
	if (int < 0)
	{
		int  r = int * -2;
		int k = r + int;

		return (k);
	}
	else
		return (int);
}
